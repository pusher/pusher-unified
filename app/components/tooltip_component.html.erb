<div class="tooltip dib relative">
  <%= render(IconComponent.new(name: 'info', title: 'Info tooltip', size: 16, classes: @classes)) %>
  <div id="<%= @id %>"
    class="tooltip--content absolute z-1 bg-black snow pa3 br2 f8 <%= @width %> <%= @position %>">
    <%= widont(content) %>
  </div>
</div>

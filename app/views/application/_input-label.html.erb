<%
  htmlFor ||= nil
  label ||= nil
  classes ||= ""

  allLabelClasses = %w(f7 mb2)
  allLabelClasses << "db" unless classes.include? "checkbox-label"
  allLabelClasses << classes unless classes.nil?
%>

<%= label_tag(
  htmlFor,
  label,
  class: allLabelClasses
) %>

